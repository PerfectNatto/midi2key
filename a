Demucs combines two processing paths: one that performs convolution directly on the raw waveform in the time domain, and another that first converts the signal into the frequency domain (spectrogram) and applies convolution there. The outputs of both paths are then combined.
