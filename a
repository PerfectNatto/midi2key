 auto& dl = executorch::extension::FileDataLoader::from("model.pte").get();
  auto& pg = executorch::runtime::Program::load(&dl).get();
  const char* name = *pg.get_method_name(0).get();           // 1 番目のメソッド
  auto& meta = pg.method_meta(name).get();
