# 1) 先頭の B をただちに除去
m = m.squeeze(0)                 # (S, C, Fr, T)   ← 4 次元になった

# 2) 実部+虚部をチャンネルに畳み込み
m = m.view(S, C // 2 * 2, Fr, T) # C が 2 の倍数前提ならそのまま

# 3) 実・虚に分割（permute や 5D view を挟まない）
real, imag = m.chunk(2, dim=1)   # どちらも (S, C//2, Fr, T)

# 4) どうしても B 次元が必要なら最後に復元
real = real.unsqueeze(0)         # (1, S, C//2, Fr, T)
imag = imag.unsqueeze(0)
